# Dependencies & build output
app/node_modules/
app/out/
app/dist/
app/*.tsbuildinfo

# Velké binárky třetích stran — uživatel si je dolije lokálně (viz README)
native/

# Release artefakty — distribuují se přes GitHub Releases, ne v repu
release/installer/*.exe
release/portable/*.exe
*.zip

# Logy / OS / IDE
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Wrangler cache
.wrangler/

# Landing page — nasazuje se lokálně přes `wrangler pages deploy pages`
# (direct upload, ne přes GitHub), takže zdroj nemusí být v repu.
/pages/
/new pages/
# Lokální zálohy bezztrátových masterů obrázků landing page (mimo deploy).
/_landing-png-src/

# Video assets (lokální, ne pro repo — pošleme přes YouTube)
how it works video/
*.mp4
*.mp3

# Náhodné screenshoty
pages/Screenshot*.png

# Web server workspace (Phase 2)
server/node_modules/
server/dist/
server/src/gen/
server/data/
server/.env
